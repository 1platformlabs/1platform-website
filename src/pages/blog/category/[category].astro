---
import BlogCategory from '@page-content/BlogCategory.astro';
import { categoryPaths } from '@i18n/collections';

export async function getStaticPaths() {
  return categoryPaths('en');
}

const { posts, category, siblings } = Astro.props;
---

<BlogCategory posts={posts} category={category} siblings={siblings} />
