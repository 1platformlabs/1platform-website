# build output
dist/

# cPanel publish channel build artefacts (see deploy/cpanel/)
cpanel-dist/
app.zip
app.zip.sha256
latest.json

# test artefacts
test-results/
playwright-report/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production
# Local credentials for scripts (QA logins for scripts/capture-product-media.mjs).
# `.env` alone did not cover them: a `.env.local` sat one `git add` away from
# being committed with a password inside.
.env.local
.env*.local

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# visual snapshots: only the container's Linux render is authoritative — a
# local darwin baseline goes green against itself and hides regressions
tests/visual/**/*-darwin.png
