name: CI — Pull Request

# The repository's five real gates, running BEFORE merge.
#
# Hasta que existió este archivo no corría ninguna: `prod.yml` dispara sólo en
# `push: [main]` y su job de build ejecuta `astro build` y nada más. O sea que
# un PR que rompía el guard de proveedores —el que enforcea la regla crítica
# del ecosistema, cero nombres de proveedor en superficie de cliente— mergeaba
# y desplegaba en verde, y lo mismo un error de tipos, porque `astro build` no
# typechequea.
#
# Deliberadamente NO se le agrega un trigger `pull_request` a `prod.yml`: ese
# workflow es la tubería de despliegue y darle ese disparador arrastraría el
# environment de PROD y sus credenciales a cada pull request.
#
# Molde: el repo hermano `1platform-api-developer` (`.github/workflows/ci.yml`).
on:
  # Stacked PRs are first-class in this repo. Restricting this workflow to
  # `main` leaves a PR against its feature dependency with no checks at all.
  pull_request:

# Ningún job necesita escribir nada.
permissions: {}

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Tipos, guard de diseño y build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v5

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Instalar dependencias
        shell: bash
        run: |
          set -euo pipefail
          npm ci --no-audit --no-fund

      # `astro build` transpila sin typechequear, así que un error de tipos
      # llegaba a producción compilando en verde.
      - name: Typecheck
        shell: bash
        run: |
          set -euo pipefail
          npm run typecheck

      # El build va ANTES del guard a propósito: dos de sus reglas —el
      # presupuesto de 64 KB de JS público y el «motion CSS-first»— miden lo que
      # el build emite, y sin él el guard aborta diciendo que no encuentra el
      # directorio de assets (medido: `npm run check` sobre un árbol limpio sale
      # 1 por eso solo).
      #
      # Con el adapter de Node ese directorio pasó a ser `dist/client/_astro`.
      # La regla resuelve las dos ubicaciones, así que sigue midiendo lo mismo;
      # lo que NO cambió, y es lo que importa, es que ante un árbol sin construir
      # falla en vez de contar cero y llamarlo pase.
      - name: Build
        shell: bash
        run: |
          set -euo pipefail
          npm run build

      # `npm run check` es `check-tells.sh && check-tells-self-test.sh`: el
      # guard y la prueba de que el guard todavía FALLA ante cada tell. Un
      # guard sólo es evidencia si se lo vio fallar.
      - name: Guard de sistema de diseño y proveedores (con su self-test)
        shell: bash
        run: |
          set -euo pipefail
          npm run check

  browser:
    name: Suite de navegador
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v5

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Instalar dependencias
        shell: bash
        run: |
          set -euo pipefail
          npm ci --no-audit --no-fund

      # Sólo chromium: es el único proyecto declarado en playwright.config.ts.
      - name: Instalar navegador
        shell: bash
        run: |
          set -euo pipefail
          npx playwright install --with-deps chromium

      # El `webServer` del config corre `npm run build && node
      # dist/server/entry.mjs`, así que esta suite mide el SERVIDOR real, que es
      # el artefacto que corre en producción detrás de nginx. Antes medía el
      # `dist/` estático que se rsynceaba; con el adapter esa frase dejó de ser
      # cierta, y la suite se reescribió para seguir al sujeto en vez de
      # ablandarse. Mide más que antes: los 301, el 404 y el endpoint de
      # imágenes los contesta ahora el mismo código que producción.
      # La línea base visual queda fuera a propósito (`testIgnore`): compara
      # renders de Linux por contenedor y es su propio comando (job `visual`).
      - name: Tests de navegador
        shell: bash
        run: |
          set -euo pipefail
          npm test

  visual:
    name: Multitenant SSR visual regression
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v5

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: ".nvmrc"

      # The npm script owns the exact browser image used to write the committed
      # Linux baselines. Running that same image here avoids approving a render
      # merely because the runner image changed its fonts or text shaper.
      - name: Comparar capturas Linux contra la línea base
        shell: bash
        run: |
          set -euo pipefail
          npm run test:visual

  baseline:
    name: Byte-for-byte non-regression
    runs-on: ubuntu-latest
    permissions:
      contents: read
    # ⚠️ NO BLOQUEANTE A PROPÓSITO (issue #99 la cablea; no le pide bloquear).
    #
    # Lo que decía este comentario —«hoy da 51 idénticas / 2 con diferencia /
    # 47 redirecciones-o-404, y por eso no puede bloquear»— YA NO ES CIERTO:
    # issue #112 re-congeló la línea base contra lo que el servidor sirve hoy,
    # y con eso el comparador da 100/100 idénticas. La compuerta volvió a
    # informar: un rojo acá vuelve a significar «este cambio movió el sitio».
    #
    # `continue-on-error` se MANTIENE, y ahora por otra razón: cambiar la
    # semántica de merge no es alcance de #112, y este job tampoco es contexto
    # requerido de `main` (lo son `Suite de navegador` y `Tipos, guard de
    # diseño y build`). Volverlo bloqueante es una decisión de una persona.
    # Mientras tanto un fallo no pasa desapercibido: el paso emite una
    # anotación `::error::`, que sale en la pestaña de checks del PR aunque el
    # job termine en verde.
    #
    # Medido al re-congelar: sabotear `BaseLayout.astro` (una plantilla
    # compartida) mueve «differing» de 0 a 53 y «exit» de 0 a 1; quitar un
    # redirect de `astro.config.mjs` y cambiarle el destino a otro los reporta
    # como `WRONG … status 404, baseline says 301` y `redirects to /pricing/,
    # baseline says /for-developers/`. La compuerta SÍ discrimina.
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v5

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Instalar dependencias
        shell: bash
        run: |
          set -euo pipefail
          npm ci --no-audit --no-fund

      # `check-no-regression.sh` construye UNA vez y sirve en modo `repo`, el
      # único que este job puede servir sin una API. Los bodies de la línea
      # base están commiteados en `tests/baseline/html/`, así que ya no hace
      # falta ni la historia completa ni reconstruir un commit viejo (#112).
      - name: No-regresión byte a byte de 1platform.pro
        shell: bash
        run: |
          set -uo pipefail
          if npm run check:baseline -- --explain; then
            exit 0
          fi
          # La anotación es lo que hace legible a una compuerta no bloqueante:
          # sin ella el veredicto vive en el log y nadie lo abre.
          echo "::error title=Regresión byte a byte de 1platform.pro::El sitio servido ya no coincide con tests/baseline/. Si el cambio es deliberado, re-congelá la línea base (ver la cabecera de scripts/check-no-regression.sh) y commiteá el diff de tests/baseline/ — ese diff ES la declaración de qué se empieza a servir."
          exit 1

  # ---------------------------------------------------------------------------
  # SonarCloud — SOLO en pull requests (epica `sonar-solo-en-pr`).
  #
  # El `if:` va en el JOB y no en el `on:` a proposito. Lo que cuesta cuota es
  # el analisis de `main`: el `nclocCount` que factura la organizacion es la
  # suma del `ncloc` de las ramas `main`, y un proyecto con PRs analizados y
  # `main` sin analizar aporta 0. Por eso este job no corre fuera de un PR, y
  # agregarle un disparador de `push` reintroduce exactamente el costo que la
  # epica retiro.
  #
  # INFORMATIVO: Sonar no es contexto requerido de `main` en este repo y este
  # job no lo vuelve requerido (`continue-on-error` en el escaneo). El veredicto
  # se publica igual, como check-run propio de SonarCloud.
  #
  # El sufijo del `name:` es deliberado: la app de SonarCloud publica su propio
  # check-run llamado exactamente `SonarCloud Code Analysis`, y sin el sufijo
  # hay dos checks homonimos de apps distintas en el mismo commit.
  # ---------------------------------------------------------------------------
  sonarcloud:
    name: SonarCloud Code Analysis (Actions — informational)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # OBLIGATORIO. Sin `permissions:` propio el job hereda el `permissions: {}`
    # del workflow (cuando lo hay): un GITHUB_TOKEN sin alcance, y el checkout
    # falla con 403 antes de que el escaner corra.
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v5
        with:
          # El analisis de codigo NUEVO se apoya en `git blame`: con un checkout
          # superficial SonarCloud no sabe que lineas trae este PR.
          fetch-depth: 0

      - name: Check for SONAR_TOKEN
        id: sonar_token
        shell: bash
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          IS_FORK: ${{ github.event.pull_request.head.repo.fork }}
          # Dependabot no ve los secretos de Actions: los suyos viven en otro
          # espacio de nombres, y darle el token reabre la exfiltracion desde el
          # build de una dependencia.
          ES_DEPENDABOT: ${{ github.actor == 'dependabot[bot]' }}
        run: |
          set -euo pipefail
          if [ -n "${SONAR_TOKEN:-}" ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          elif [ "${ES_DEPENDABOT:-false}" = "true" ] || [ "${IS_FORK:-false}" = "true" ]; then
            # Los dos casos en que el token NO llega por diseno de GitHub: el
            # autor no puede hacer nada para que aparezca.
            echo "present=false" >> "$GITHUB_OUTPUT"
            if [ "${ES_DEPENDABOT:-false}" = "true" ]; then
              MOTIVO="el PR lo abrio Dependabot, que por diseno no recibe los secretos de Actions"
            else
              MOTIVO="el PR viene de un fork, que por diseno no recibe los secretos"
            fi
            echo "::warning::SonarCloud NO analizo este PR: ${MOTIVO}."
            {
              echo "## SonarCloud NO analizo este PR"
              echo
              echo "Este check esta en verde y **no midio nada**."
              echo
              echo "**Motivo:** ${MOTIVO}."
            } >> "$GITHUB_STEP_SUMMARY"
          else
            # Cualquier otro PR sin token es un defecto de configuracion: el
            # secreto vive en la organizacion. Falla en vez de pasar en falso.
            echo "::error::SONAR_TOKEN no esta disponible en este job y el PR no viene de un fork ni de Dependabot. El analisis NO corrio."
            exit 1
          fi

      - name: SonarCloud Scan
        if: steps.sonar_token.outputs.present == 'true'
        # Pinneada al SHA, no al tag: `githubactions:S7637` — un tag movil deja
        # que el duenio de la accion cambie lo que corre en un job que recibe
        # `SONAR_TOKEN` y `GITHUB_TOKEN`.
        uses: SonarSource/sonarcloud-github-action@ffc3010689be73b8e5ae0c57ce35968afd7909e8 # v5
        continue-on-error: true
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # El host, EXPLICITO. Sin esto el escaner CLI 7.x pide
          # `/api/v2/analysis/jres`, `sonarcloud.io` responde 404 y el fallo se
          # lee como un problema de credenciales. Es INTERMITENTE: si el runner
          # tiene el escaner en cache no baja el JRE y no falla.
          SONAR_HOST_URL: https://sonarcloud.io
        with:
          # Espera el veredicto: sin esto el paso termina en verde apenas sube el
          # analisis, antes de que exista el resultado del gate.
          args: >
            -Dsonar.qualitygate.wait=true
            -Dsonar.qualitygate.timeout=600
